Django==6.1.1
pytest-django==4.14.0

# Flask/SQLAlchemy: se retiran en la Fase 8 de la migración a Django (ver
# /home/cokie/.claude/plans/kind-napping-grove.md), una vez portado el
# último módulo. Hasta entonces la app vieja sigue corriendo con esto.
Flask==3.1.3
SQLAlchemy==2.0.52

openpyxl==3.1.5
python-docx==1.2.0
docxtpl==0.20.2
